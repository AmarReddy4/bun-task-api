{
  "name": "bun-task-api",
  "version": "1.0.0",
  "description": "A simple task management REST API built with Bun 1.0 and Hono",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts"
  },
  "dependencies": {
    "hono": "^3.6.0"
  },
  "devDependencies": {
    "@types/bun": "^1.0.0"
  }
}
